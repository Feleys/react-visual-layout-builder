enum backgroundImageSize {
  COVER = "cover",
  CONTAIN = "contain",
  CUSTOM_SIZE = "custom_size"
}

enum backgroundImagePosition {
  TOP = "top",
  BOTTOM = "bottom",
  LEFT = "left",
  RIGHT = "right",
  CENTER = "center"
}

export { backgroundImageSize, backgroundImagePosition };
